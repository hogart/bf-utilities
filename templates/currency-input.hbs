<div class="form-group currency-input">
  <label for="{{currency.name}}">
    <img src="{{currency.img}}" alt="" aria-hidden="true"/>
    {{currency.label}}
  </label>
  <input type="number" id="{{currency.name}}" name="{{currency.name}}" value="0" step="1"/>
</div>